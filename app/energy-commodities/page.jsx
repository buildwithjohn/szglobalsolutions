import EnergyClient from '@/components/energy/EnergyClient';

export const metadata = {
  title: 'Energy & Commodities — Products & Market Access',
  description:
    'Commercial opportunities across downstream energy and selected commodity markets — EN590 diesel, Jet A-1, LPG, crude oil, petrochemicals and selected commodities, structured and connected to trusted counterparties.',
};

export default function EnergyCommoditiesPage() {
  return <EnergyClient />;
}
